# Assets

| File | Used by | Notes |
|---|---|---|
| `hero.png` | `README.md` | Product banner — dashboard on desktop, sign-in on mobile. Export at 2× (around 2400px wide) so it stays sharp on high-DPI screens, and keep it under ~500 KB. |

Screenshots go stale faster than code. If the UI changes materially, re-export
rather than leaving a banner that shows a version nobody is running.
